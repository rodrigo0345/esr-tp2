package bootstraper

type Lib struct {
  // store the IP of where the video is
  videoMapper map[string]string
}

func Bootstraper(){
  // add server with given video
  // get to the client the server with the given video
}
